import styled from 'styled-components';

import { StyledInputMessageProps } from './interface';

export const InputWrapper = styled.div``;

export const InputMessage = styled.p<StyledInputMessageProps>`
  font-size: 16px;
  padding: 7px 16px 0;
`;
